# Development dependencies - not required for normal installation
# These dependencies are for development purposes only
# For production use, dependencies are managed through setup.py

# Core dependencies (these match setup.py install_requires)
pandas>=1.3.0
flask>=2.0.0
psycopg2-binary>=2.9.0
click>=8.0.0

# Additional development dependencies
gunicorn>=20.1.0
gevent>=21.12.0 